<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms" xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:materialDesign="clr-namespace:NativeCode.Mobile.Controls.MaterialDesign;assembly=NativeCode.Mobile.Controls.MaterialDesign"
             x:Class="Demo.MainPage" IsBusy="{Binding IsBusy}" Title="{Binding Title}">
  <StackLayout>
    <materialDesign:CheckBox />
    <StackLayout Orientation="Horizontal" HorizontalOptions="FillAndExpand" VerticalOptions="Center">
      <Label Text="Switch on/off" />
      <Switch />
    </StackLayout>
    <materialDesign:FlatButton Text="Hit me!" />
    <Button Text="Hit me twice!" />
  </StackLayout>
</ContentPage>
